# คู่มือการ Deploy ผ่าน Vercel และตั้งค่า Domain ที่ Namecheap t

## 📋 สารบัญ

1. [การ Deploy ผ่าน Vercel](#การ-deploy-ผ่าน-vercel)
2. [การจด Domain ที่ Namecheap](#การจด-domain-ที่-namecheap)
3. [การตั้งค่า DNS ที่ Namecheap](#การตั้งค่า-dns-ที่-namecheap)
4. [การเชื่อม Domain กับ Vercel](#การเชื่อม-domain-กับ-vercel)
5. [การตรวจสอบและ Troubleshooting](#การตรวจสอบและ-troubleshooting)

---

## 🚀 การ Deploy ผ่าน Vercel

### ขั้นตอนที่ 1: เตรียมโปรเจกต์

1. **ตรวจสอบว่าโปรเจกต์พร้อม deploy:**
   ```bash
   npm run build
   ```
   ถ้า build สำเร็จ แสดงว่าโปรเจกต์พร้อม deploy

2. **Commit โค้ดทั้งหมด:**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

### ขั้นตอนที่ 2: สร้างบัญชี Vercel

1. ไปที่ [https://vercel.com](https://vercel.com)
2. คลิก **"Sign Up"** หรือ **"Log In"**
3. เลือกวิธีสมัครสมาชิก:
   - **แนะนำ:** ใช้ GitHub account (ง่ายและสะดวกที่สุด)
   - หรือใช้ Email

### ขั้นตอนที่ 3: สร้างโปรเจกต์ใหม่บน Vercel

1. หลังจาก Login แล้ว คลิก **"Add New..."** → **"Project"**
2. **Import Git Repository:**
   - เลือก Git provider (GitHub, GitLab, Bitbucket)
   - เลือก repository ที่ต้องการ deploy
   - คลิก **"Import"**

### ขั้นตอนที่ 4: ตั้งค่า Build Configuration

Vercel จะ auto-detect Vite project โดยอัตโนมัติ แต่ให้ตรวจสอบว่า:

- **Framework Preset:** Vite
- **Root Directory:** `.` (หรือปล่อยว่างไว้)
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

### ขั้นตอนที่ 5: ตั้งค่า Environment Variables

**สำคัญมาก!** ต้องตั้งค่า Environment Variables ก่อน deploy:

1. ในหน้า Project Settings → **Environment Variables**
2. คลิก **"Add New"** เพื่อเพิ่มตัวแปรใหม่

**ตัวแปรที่ต้องเพิ่ม:**

**ตัวแปรที่ 1:**
- **Key:** `VITE_SUPABASE_URL`
- **Value:** ใส่ URL ของ Supabase project ของคุณ (เช่น `https://xxxxx.supabase.co`)
- **Environment:** เลือกทั้งหมด (Production, Preview, Development)
- คลิก **Save**

**ตัวแปรที่ 2:**
- **Key:** `VITE_SUPABASE_ANON_KEY`
- **Value:** ใส่ Anon Key ของ Supabase project ของคุณ
- **Environment:** เลือกทั้งหมด (Production, Preview, Development)
- คลิก **Save**

**ตัวอย่างการกรอก:**
```
Key: VITE_SUPABASE_URL
Value: https://abcdefghijklmnop.supabase.co

Key: VITE_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### ขั้นตอนที่ 6: Deploy

1. คลิก **"Deploy"**
2. รอให้ build เสร็จ (ประมาณ 1-3 นาที)
3. เมื่อเสร็จแล้ว จะได้ลิงก์ Production URL เช่น:
   ```
   https://your-project-name.vercel.app
   ```

### ขั้นตอนที่ 7: Auto Deploy (ตั้งค่าอัตโนมัติ)

หลังจาก deploy ครั้งแรกแล้ว:
- ทุกครั้งที่คุณ **push code ไปที่ GitHub** → Vercel จะ **deploy อัตโนมัติ**
- ถ้า push ไปที่ `main` branch → จะ deploy ไปที่ **Production**
- ถ้า push ไปที่ branch อื่น → จะได้ **Preview URL** แยก

---

## 🌐 การจด Domain ที่ Namecheap

### ขั้นตอนที่ 1: สร้างบัญชี Namecheap

1. ไปที่ [https://www.namecheap.com](https://www.namecheap.com)
2. คลิก **"Sign Up"** ที่มุมขวาบน
3. กรอกข้อมูล:
   - Email
   - Password
   - ชื่อ-นามสกุล
   - ประเทศ
4. ยืนยันอีเมล (ตรวจสอบอีเมลและคลิกลิงก์ยืนยัน)

### ขั้นตอนที่ 2: ค้นหา Domain ที่ต้องการ

1. หลังจาก Login แล้ว ไปที่หน้าแรก
2. ในช่องค้นหา Domain พิมพ์ชื่อ domain ที่ต้องการ (เช่น `mywebsite.com`)
3. คลิก **"Search"**
4. ระบบจะแสดง:
   - Domain ที่ว่าง (Available) - สามารถซื้อได้
   - Domain ที่มีคนใช้แล้ว (Taken) - ไม่สามารถซื้อได้

### ขั้นตอนที่ 3: เลือก Domain

1. เลือก domain ที่ต้องการ (เช่น `.com`, `.net`, `.org`)
2. **คำแนะนำ:**
   - `.com` = ราคาแพงที่สุด แต่เป็นที่นิยมมากที่สุด
   - `.net` = ราคาปานกลาง
   - `.org` = เหมาะสำหรับองค์กร
   - `.co` = ราคาถูกกว่า .com
   - `.xyz` = ราคาถูกมาก

3. คลิก **"Add to Cart"** สำหรับ domain ที่ต้องการ

### ขั้นตอนที่ 4: ตั้งค่า Domain

1. ในหน้า Shopping Cart:
   - เลือกระยะเวลา (1 ปี, 2 ปี, 3 ปี)
   - **WhoisGuard** (แนะนำให้เปิด - ป้องกันข้อมูลส่วนตัว)
   - **Auto-Renew** (แนะนำให้เปิด - จะต่ออายุอัตโนมัติ)

2. คลิก **"Checkout"**

### ขั้นตอนที่ 5: ชำระเงิน

1. กรอกข้อมูลการชำระเงิน:
   - หมายเลขบัตรเครดิต/เดบิต
   - วันหมดอายุ
   - CVV
   - ชื่อบนบัตร

2. ตรวจสอบข้อมูลทั้งหมด
3. คลิก **"Pay Now"**

### ขั้นตอนที่ 6: ยืนยันการซื้อ

1. หลังจากชำระเงินสำเร็จ จะได้รับอีเมลยืนยัน
2. ไปที่ **"Domain List"** ใน Namecheap Dashboard
3. จะเห็น domain ที่ซื้อแล้ว

---

## ⚙️ การตั้งค่า DNS ที่ Namecheap

### ขั้นตอนที่ 1: เข้าสู่ระบบ Namecheap

1. Login ที่ [https://www.namecheap.com](https://www.namecheap.com)
2. ไปที่ **"Domain List"** (เมนูด้านบน)

### ขั้นตอนที่ 2: เลือก Domain ที่ต้องการตั้งค่า

1. คลิกที่ domain ที่ต้องการตั้งค่า
2. หรือคลิก **"Manage"** ข้าง domain

### ขั้นตอนที่ 3: ไปที่หน้า DNS Settings

1. ในหน้า Domain Management คลิกแท็บ **"Advanced DNS"**
2. หรือไปที่ **"Domain"** → **"Advanced DNS"**

### ขั้นตอนที่ 4: ตั้งค่า DNS Records สำหรับ Vercel

**วิธีที่ 1: ใช้ A Record และ CNAME Record (แนะนำ)**

1. **ลบ DNS Records เดิม** (ถ้ามี):
   - คลิก **"Remove"** ที่ records เก่าทั้งหมด

2. **เพิ่ม A Record สำหรับ Root Domain (เช่น example.com):**
   - คลิก **"Add New Record"**
   - เลือก **"A Record"**
   - **Host:** `@` (หรือปล่อยว่าง)
   - **Value:** `76.76.21.21` (IP ของ Vercel)
   - **TTL:** `Automatic` (หรือ `30 min`)
   - คลิก **"Save"** (เครื่องหมายถูก)

3. **เพิ่ม CNAME Record สำหรับ www (เช่น www.example.com):**
   - คลิก **"Add New Record"**
   - เลือก **"CNAME Record"**
   - **Host:** `www`
   - **Value:** `cname.vercel-dns.com` (หรือใช้ค่า CNAME ที่ Vercel ให้มา)
   - **TTL:** `Automatic` (หรือ `30 min`)
   - คลิก **"Save"**

**วิธีที่ 2: ใช้ Nameservers ของ Vercel (ง่ายกว่า - แนะนำ)**

1. **ไปที่ Vercel Dashboard:**
   - ไปที่ Project ของคุณ
   - ไปที่ **Settings** → **Domains**
   - เพิ่ม domain ที่ต้องการ (เช่น `example.com` และ `www.example.com`)

2. **Copy Nameservers จาก Vercel:**
   - Vercel จะแสดง Nameservers เช่น:
     ```
     ns1.vercel-dns.com
     ns2.vercel-dns.com
     ```

3. **ตั้งค่า Nameservers ที่ Namecheap:**
   - กลับไปที่ Namecheap
   - ไปที่ Domain Management → **"Nameservers"**
   - เลือก **"Custom DNS"**
   - ใส่ Nameservers จาก Vercel:
     ```
     ns1.vercel-dns.com
     ns2.vercel-dns.com
     ```
   - คลิก **"Save"**

**หมายเหตุ:** วิธีนี้จะให้ Vercel จัดการ DNS ทั้งหมดให้อัตโนมัติ (ง่ายกว่า)

---

## 🔗 การเชื่อม Domain กับ Vercel

### ขั้นตอนที่ 1: เพิ่ม Domain ใน Vercel

1. ไปที่ Vercel Dashboard
2. เลือก Project ของคุณ
3. ไปที่ **Settings** → **Domains**
4. คลิก **"Add Domain"**

### ขั้นตอนที่ 2: ใส่ Domain

1. พิมพ์ domain ที่ต้องการ (เช่น `example.com`)
2. คลิก **"Add"**
3. Vercel จะแสดง:
   - **Configuration** (วิธีตั้งค่า DNS)
   - **Nameservers** (ถ้าใช้วิธี Nameservers)
   - **DNS Records** (ถ้าใช้วิธี DNS Records)

### ขั้นตอนที่ 3: เลือกวิธีตั้งค่า

**วิธีที่ 1: ใช้ Nameservers (แนะนำ - ง่ายที่สุด)**

1. Copy Nameservers จาก Vercel:
   ```
   ns1.vercel-dns.com
   ns2.vercel-dns.com
   ```

2. ไปตั้งค่าที่ Namecheap (ตามขั้นตอนในส่วน "การตั้งค่า DNS ที่ Namecheap")

3. รอ DNS Propagation (ประมาณ 5 นาที - 48 ชั่วโมง)

**วิธีที่ 2: ใช้ DNS Records**

1. Copy DNS Records จาก Vercel:
   - **A Record:** `@` → `76.76.21.21`
   - **CNAME Record:** `www` → `cname.vercel-dns.com`

2. ไปตั้งค่าที่ Namecheap (ตามขั้นตอนในส่วน "การตั้งค่า DNS ที่ Namecheap")

3. รอ DNS Propagation (ประมาณ 5 นาที - 48 ชั่วโมง)

### ขั้นตอนที่ 4: ตั้งค่า Domain สำหรับ www

1. ในหน้า Domains ของ Vercel
2. คลิก **"Add Domain"** อีกครั้ง
3. พิมพ์ `www.example.com` (ใส่ www นำหน้า)
4. Vercel จะตั้งค่าให้อัตโนมัติ

### ขั้นตอนที่ 5: รอ DNS Propagation

1. หลังจากตั้งค่า DNS แล้ว ต้องรอให้ DNS propagate
2. **เวลาที่ใช้:**
   - **เร็วที่สุด:** 5-10 นาที
   - **ปกติ:** 1-24 ชั่วโมง
   - **นานที่สุด:** 48 ชั่วโมง

3. **ตรวจสอบสถานะ:**
   - ไปที่ Vercel Dashboard → Domains
   - จะเห็นสถานะ:
     - ✅ **Valid Configuration** = ตั้งค่าถูกต้อง
     - ⏳ **Pending** = กำลังรอ DNS propagate
     - ❌ **Invalid Configuration** = ตั้งค่าผิดพลาด

---

## ✅ การตรวจสอบและ Troubleshooting

### ตรวจสอบว่า DNS ตั้งค่าถูกต้อง

**วิธีที่ 1: ใช้ Online Tools**

1. ไปที่ [https://dnschecker.org](https://dnschecker.org)
2. พิมพ์ domain ของคุณ (เช่น `example.com`)
3. เลือก record type (A, CNAME, NS)
4. คลิก **"Search"**
5. ตรวจสอบว่าค่าที่แสดงตรงกับที่ตั้งค่าไว้

**วิธีที่ 2: ใช้ Command Line**

```bash
# ตรวจสอบ A Record
nslookup example.com

# ตรวจสอบ CNAME Record
nslookup www.example.com

# ตรวจสอบ Nameservers
nslookup -type=NS example.com
```

**วิธีที่ 3: ใช้ Online DNS Lookup**

- [https://www.whatsmydns.net](https://www.whatsmydns.net)
- [https://mxtoolbox.com](https://mxtoolbox.com)

### ตรวจสอบว่า Domain เชื่อมกับ Vercel แล้ว

1. ไปที่ Vercel Dashboard → Domains
2. ตรวจสอบสถานะ:
   - ✅ **Valid Configuration** = พร้อมใช้งาน
   - ⏳ **Pending** = ยังรอ DNS propagate
   - ❌ **Invalid Configuration** = ต้องแก้ไข

3. **ทดสอบเปิดเว็บไซต์:**
   - เปิด browser
   - พิมพ์ domain ของคุณ (เช่น `https://example.com`)
   - ถ้าเว็บไซต์โหลดได้ = สำเร็จ!

### Troubleshooting ปัญหาที่พบบ่อย

#### ❌ ปัญหา: Domain ไม่ทำงานหลังจากตั้งค่า DNS แล้ว

**วิธีแก้:**

1. **ตรวจสอบ DNS Records:**
   - ไปที่ Namecheap → Advanced DNS
   - ตรวจสอบว่าตั้งค่าถูกต้องหรือไม่
   - ตรวจสอบว่า Value ตรงกับที่ Vercel ให้มาหรือไม่

2. **ตรวจสอบ Nameservers:**
   - ถ้าใช้ Nameservers ตรวจสอบว่าตั้งค่าถูกต้องหรือไม่
   - ต้องเป็น `ns1.vercel-dns.com` และ `ns2.vercel-dns.com`

3. **รอ DNS Propagation:**
   - DNS อาจใช้เวลา 24-48 ชั่วโมง
   - ใช้ [dnschecker.org](https://dnschecker.org) ตรวจสอบว่า DNS propagate ไปทั่วโลกแล้วหรือยัง

4. **ตรวจสอบที่ Vercel:**
   - ไปที่ Vercel Dashboard → Domains
   - ดูว่ามี error message อะไรหรือไม่
   - ถ้ามี error ให้แก้ไขตามที่แนะนำ

#### ❌ ปัญหา: www ไม่ทำงาน แต่ root domain ทำงาน

**วิธีแก้:**

1. **ตรวจสอบ CNAME Record:**
   - ไปที่ Namecheap → Advanced DNS
   - ตรวจสอบว่ามี CNAME Record สำหรับ `www` หรือไม่
   - Value ต้องเป็น `cname.vercel-dns.com` (หรือค่าที่ Vercel ให้มา)

2. **เพิ่ม www Domain ใน Vercel:**
   - ไปที่ Vercel Dashboard → Domains
   - เพิ่ม `www.example.com` (ใส่ www นำหน้า)

#### ❌ ปัญหา: SSL Certificate ไม่ทำงาน

**วิธีแก้:**

1. **รอให้ Vercel สร้าง SSL Certificate:**
   - Vercel จะสร้าง SSL Certificate อัตโนมัติหลังจาก DNS propagate แล้ว
   - ใช้เวลา 5-10 นาที

2. **ตรวจสอบที่ Vercel:**
   - ไปที่ Domains → คลิกที่ domain
   - ดูสถานะ SSL Certificate

3. **ถ้ายังไม่ทำงาน:**
   - ลบ domain ออกจาก Vercel
   - รอ 5 นาที
   - เพิ่ม domain ใหม่

#### ❌ ปัญหา: Domain Redirect ไม่ทำงาน

**วิธีแก้:**

1. **ตรวจสอบ Redirect Settings:**
   - ไปที่ Vercel Dashboard → Domains
   - ตรวจสอบว่า redirect ตั้งค่าถูกต้องหรือไม่

2. **ตั้งค่า Redirect ใน Vercel:**
   - ถ้าต้องการ redirect `www` → `non-www` หรือ `non-www` → `www`
   - ไปที่ Settings → Domains
   - เลือก domain ที่ต้องการ redirect
   - ตั้งค่า redirect

---

## 📝 สรุปขั้นตอนทั้งหมด

### 1. Deploy บน Vercel
- ✅ สร้างบัญชี Vercel
- ✅ Import Git Repository
- ✅ ตั้งค่า Environment Variables
- ✅ Deploy

### 2. จด Domain ที่ Namecheap
- ✅ สร้างบัญชี Namecheap
- ✅ ค้นหาและซื้อ Domain
- ✅ ชำระเงิน

### 3. ตั้งค่า DNS
- ✅ ไปที่ Namecheap → Advanced DNS
- ✅ ตั้งค่า Nameservers หรือ DNS Records ตามที่ Vercel แนะนำ

### 4. เชื่อม Domain กับ Vercel
- ✅ เพิ่ม Domain ใน Vercel Dashboard
- ✅ รอ DNS Propagation (5 นาที - 48 ชั่วโมง)
- ✅ ทดสอบเปิดเว็บไซต์

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation - Domains](https://vercel.com/docs/concepts/projects/domains)
- [Namecheap Dashboard](https://www.namecheap.com/myaccount/)
- [Namecheap DNS Management Guide](https://www.namecheap.com/support/knowledgebase/article.aspx/767/10/how-to-change-dns-for-a-domain/)
- [DNS Checker](https://dnschecker.org)
- [What's My DNS](https://www.whatsmydns.net)

---

## ⚠️ หมายเหตุสำคัญ

1. **DNS Propagation:**
   - ใช้เวลา 5 นาที - 48 ชั่วโมง
   - ต้องรอให้ DNS propagate ก่อน domain จะทำงาน

2. **SSL Certificate:**
   - Vercel จะสร้าง SSL Certificate อัตโนมัติ
   - ใช้เวลา 5-10 นาทีหลังจาก DNS propagate แล้ว

3. **Environment Variables:**
   - อย่าลืมตั้งค่า Environment Variables ใน Vercel
   - อย่า commit `.env` file ขึ้น Git

4. **Auto Deploy:**
   - ทุกครั้งที่ push code ไป GitHub → Vercel จะ deploy อัตโนมัติ
   - ไม่ต้อง deploy เองทุกครั้ง

5. **Domain Renewal:**
   - Domain จะหมดอายุทุกปี (หรือตามที่ซื้อ)
   - เปิด Auto-Renew เพื่อต่ออายุอัตโนมัติ

---

**🎉 เสร็จแล้ว! ตอนนี้คุณสามารถ deploy เว็บไซต์ผ่าน Vercel และใช้ domain ของตัวเองได้แล้ว!**

